<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/flSoundIconContainer"
        android:layout_width="62dp"
        android:layout_height="62dp"
        android:padding="3dp">

        <FrameLayout
            android:id="@+id/flSoundIconClip"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_circle_clip"
            android:clipToOutline="true">

            <ImageView
                android:id="@+id/ivSoundIcon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:importantForAccessibility="no"
                android:scaleType="fitCenter" />

            <TextView
                android:id="@+id/tvAdTag"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:layout_gravity="bottom"
                android:background="#D93B85FC"
                android:gravity="center"
                android:text="AD"
                android:textColor="@android:color/white"
                android:textSize="11sp"
                android:textStyle="bold"
                android:visibility="gone" />

        </FrameLayout>

        <ImageView
            android:id="@+id/ivHotTag"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="top|start"
            android:layout_marginStart="-2dp"
            android:layout_marginTop="-2dp"
            android:importantForAccessibility="no"
            android:src="@mipmap/img_clap_alarm_setting_hot_tag"
            android:scaleType="centerInside"
            android:visibility="gone" />

    </FrameLayout>

    <TextView
        android:id="@+id/tvSoundName"
        android:layout_width="56dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/text_dark"
        android:textSize="14sp" />

</LinearLayout>
